import {
  IconSunFilled,
  IconMoonFilled,
  IconX,
  IconSearch,
} from "@tabler/icons-react";

const LightModeIcon = IconSunFilled;
const DarkModeIcon = IconMoonFilled;
const CloseIcon = IconX;
const SearchIcon = IconSearch;
export { LightModeIcon, DarkModeIcon, CloseIcon, SearchIcon };
